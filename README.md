# Castle

Electron + React + Vite + TypeScript scaffold

Available scripts:

- `npm run dev` - inicia o Vite + tsc watch + Electron (desenvolvimento)
- `npm run build` - build do renderer + compilação do Electron + electron-builder
